#include <bits/stdc++.h>


using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    vector<int> v = {2,1,5,3,10,6};
    sort(v.begin(), v.end());

    for(int i=0; i < v.size(); i++)
    {
        cout<< v[i] << "\n";
    }

    vector<pair<int, int>> v2;

    v2.push_back({1, 3});

}